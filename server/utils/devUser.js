// Temporary development-only user identifier.
// Replace this with Set A authentication/JWT user identity when Set A is integrated.
export const DEV_USER_ID = 'dev-user';

export const getTemporaryUserId = () => DEV_USER_ID;
